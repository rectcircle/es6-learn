{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "http-server",
            "type": "shell",
            "command": "http-server",
            "args": [
                "${workspaceFolder}/src/ch19-module/"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}